<speak>
    {{#if services.edges.length }}
    <s>
        {{_ 'Here is what I\'m offering:'}}
        {{#each services.edges}}
            {{ this.node.name }}<break />
        {{/each}}
    </s>
    {{else}}
    <s>
        {{_ 'I\'m not offering anything at the moment'}}
    </s>
    {{/if}}
</speak>
